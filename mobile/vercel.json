{
  "buildCommand": "npm ci && npm run export:web",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
