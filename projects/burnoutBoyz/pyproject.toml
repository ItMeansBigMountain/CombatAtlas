[project]
name = "burnoutboyz-backend"
version = "0.1.0"
description = "Provenance-first vehicle maintenance domain backend"
requires-python = ">=3.11"

[project.scripts]
burnoutboyz-admin = "burnoutboyz.admin:main"

[tool.pyright]
include = ["burnoutboyz", "tests"]
