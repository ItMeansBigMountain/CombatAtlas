# Kanban consolidation backup

Created before consolidating Hermes Kanban boards into the canonical `default` board on 2026-05-29.

Contents:

- `default-kanban.db` — the original empty default board DB before consolidation.
- `kanban-dir/boards/hermez/` — the former HeRmEz board data copied before deletion.
- `kanban-dir/current` — active board marker at backup time.

The `nous-kanban-demo` board was intentionally not retained here because the user explicitly said demo boards were not needed.
