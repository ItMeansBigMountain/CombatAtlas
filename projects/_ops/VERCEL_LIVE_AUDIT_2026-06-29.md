# Vercel Live Audit — 2026-06-29

Projects visible via API: **57**
Primary URL healthy 200: **41**
Primary URL errors/non-resolving: **16**

## Needs attention
- **local-meeting-transcriber-frontend** — primary `local-meeting-transcriber-frontend.vercel.app` returned `404`; latest state `None`; framework `None`.
- **codology** — primary `codology.vercel.app` returned `404`; latest state `READY`; framework `None`.
- **codology-api** — primary `codology-api.vercel.app` returned `404`; latest state `READY`; framework `express`.
- **stock_news_backend** — primary `stock_news_backend.vercel.app` returned `None`; latest state `READY`; framework `None`.
- **api.requests** — primary `api.requests.vercel.app` returned `None`; latest state `READY`; framework `vite`.
- **cellphone_scripts** — primary `cellphone_scripts.vercel.app` returned `None`; latest state `READY`; framework `vite`.
- **deployment_docs** — primary `deployment_docs.vercel.app` returned `None`; latest state `READY`; framework `vite`.
- **journal-ai** — primary `journal-ai.vercel.app` returned `404`; latest state `READY`; framework `vite`.
- **music** — primary `music.vercel.app` returned `None`; latest state `READY`; framework `vite`.
- **portfolio-sentiment-subscription-app** — primary `portfolio-sentiment-subscription-app.vercel.app` returned `404`; latest state `READY`; framework `vite`.
- **selenium** — primary `selenium.vercel.app` returned `404`; latest state `READY`; framework `vite`.
- **social-media-analysis** — primary `social-media-analysis.vercel.app` returned `404`; latest state `READY`; framework `vite`.
- **tiktok-clone** — primary `tiktok-clone.vercel.app` returned `500`; latest state `READY`; framework `vite`.
- **tweet_video_generator** — primary `tweet_video_generator.vercel.app` returned `None`; latest state `READY`; framework `vite`.
- **docs** — primary `docs.vercel.app` returned `None`; latest state `READY`; framework `vite`.
- **jupyter-notebooks** — primary `jupyter-notebooks.vercel.app` returned `404`; latest state `READY`; framework `vite`.

## Healthy primary URLs
- **honda-tech-upgrade** — `honda-tech-upgrade.vercel.app` 200; latest `READY`; `vite`.
- **journal-app** — `journal-app.vercel.app` 200; latest `READY`; `vite`.
- **musicai** — `musicai.vercel.app` 200; latest `READY`; `vite`.
- **faceless-youtube-channel** — `faceless-youtube-channel.vercel.app` 200; latest `READY`; `vite`.
- **api-requests** — `api-requests.vercel.app` 200; latest `READY`; `vite`.
- **card-intel-scanner** — `card-intel-scanner.vercel.app` 200; latest `READY`; `vite`.
- **3d-react-web** — `3d-react-web.vercel.app` 200; latest `BLOCKED`; `create-react-app`.
- **addictive-mobile-games** — `addictive-mobile-games.vercel.app` 200; latest `READY`; `vite`.
- **legacy-src** — `legacy-src.vercel.app` 200; latest `READY`; `vite`.
- **consumer-advocate-app** — `consumer-advocate-app.vercel.app` 200; latest `READY`; `vite`.
- **algos** — `algos.vercel.app` 200; latest `READY`; `vite`.
- **combatatlas** — `combatatlas.vercel.app` 200; latest `READY`; `vite`.
- **ticvoter** — `ticvoter.vercel.app` 200; latest `READY`; `None`.
- **musclemadness** — `musclemadness.vercel.app` 200; latest `READY`; `None`.
- **coding-school-platform** — `coding-school-platform.vercel.app` 200; latest `READY`; `vite`.
- **cox-elementary-pta** — `cox-elementary-pta.vercel.app` 200; latest `READY`; `vite`.
- **local-meeting-transcriber** — `local-meeting-transcriber.vercel.app` 200; latest `READY`; `vite`.
- **music-mood-app** — `music-mood-app.vercel.app` 200; latest `READY`; `vite`.
- **networking** — `networking.vercel.app` 200; latest `READY`; `vite`.
- **oyama-productions-legal** — `oyama-productions-legal.vercel.app` 200; latest `READY`; `vite`.
- **policy-pit-app** — `policy-pit-app.vercel.app` 200; latest `READY`; `vite`.
- **robinhood-email-reports** — `robinhood-email-reports.vercel.app` 200; latest `READY`; `vite`.
- **school** — `school.vercel.app` 200; latest `READY`; `vite`.
- **scraper-project** — `scraper-project.vercel.app` 200; latest `READY`; `vite`.
- **sleep-dream-app** — `sleep-dream-app.vercel.app` 200; latest `READY`; `vite`.
- **stock-news-frontend** — `stock-news-frontend.vercel.app` 200; latest `READY`; `angular`.
- **store-code-content-studio** — `store-code-content-studio.vercel.app` 200; latest `READY`; `vite`.
- **survey-analytics-website** — `survey-analytics-website.vercel.app` 200; latest `READY`; `vite`.
- **tiktok-shop-shopify-commerce** — `tiktok-shop-shopify-commerce.vercel.app` 200; latest `READY`; `vite`.
- **tournament-wager-app** — `tournament-wager-app.vercel.app` 200; latest `READY`; `vite`.
- **twitter-therapy-app** — `twitter-therapy-app.vercel.app` 200; latest `READY`; `vite`.
- **cloudautomation** — `cloudautomation.vercel.app` 200; latest `READY`; `vite`.
- **musclemadness-api** — `musclemadness-api.vercel.app` 200; latest `READY`; `vite`.
- **rts-js-chatrooms** — `rts-js-chatrooms.vercel.app` 200; latest `READY`; `vite`.
- **ticvoter-rest-api** — `ticvoter-rest-api.vercel.app` 200; latest `READY`; `vite`.
- **tutoring-repl** — `tutoring-repl.vercel.app` 200; latest `READY`; `vite`.
- **tweetbetweenthelines** — `tweetbetweenthelines.vercel.app` 200; latest `READY`; `vite`.
- **utilityscripts** — `utilityscripts.vercel.app` 200; latest `READY`; `vite`.
- **watsonai** — `watsonai.vercel.app` 200; latest `READY`; `vite`.
- **webcrawl** — `webcrawl.vercel.app` 200; latest `READY`; `vite`.
- **wuthappened** — `wuthappened.vercel.app` 200; latest `READY`; `vite`.